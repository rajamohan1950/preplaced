services:
  - type: web
    name: preplaced-clone
    env: static
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: .
    envVars:
      - key: NODE_VERSION
        value: 18

